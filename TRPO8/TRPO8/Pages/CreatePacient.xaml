<Page x:Class="TRPO8.Pages.CreatePacient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO8.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CreatePacient">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <Label Content="Фамилия пациента"/>
                <Label Content="Имя пациентов:"/>
                <Label Content="Отчество пациента:"/>
                <Label Content="Дата рождения:"/>
                <Label Content="Номер телефона:"/>
                <Button Click="AddPacientButton_Click" Content="Добавить" Margin="4"/>
                <Button Click="BackButton_Click" Content="Назад"  Margin="4"/>
            </StackPanel>
            <StackPanel>
                <TextBox Text="{Binding AddPacient.LastName}" Margin="4" Width="150px"/>
                <TextBox Text="{Binding AddPacient.Name}" Margin="4" Width="150px"/>
                <TextBox Text="{Binding AddPacient.MiddleName}" Margin="4" Width="150px"/>
                <DatePicker SelectedDate="{Binding AddPacient.Birthday}" Margin="1" Width="150"/>
                <TextBox Text="{Binding AddPacient.PhoneNumber}" Margin="4" Width="150px"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
